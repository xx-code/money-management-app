export type Account = {
    title: string;
    credit_value: number;
    credit_limit: number;
}

export type AccountDisplay = {
    id: string;
    title: string;
    credit_value: number;
    credit_limit: number;
    balance: number;
}